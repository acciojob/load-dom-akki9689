//your JS code here. If required.
 function handleDOMLoad() {
            // Write "DOM load success" when the DOM is loaded
            console.log("DOM load success");
        }

        document.addEventListener("DOMContentLoaded", handleDOMLoad);
